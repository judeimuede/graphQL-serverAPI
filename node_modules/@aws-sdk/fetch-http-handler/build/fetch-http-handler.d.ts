import { BrowserHttpOptions, HttpHandler, HttpHandlerOptions, HttpRequest, HttpResponse } from "@aws-sdk/types";
export declare class FetchHttpHandler implements HttpHandler<Blob, BrowserHttpOptions> {
    private readonly httpOptions;
    constructor(httpOptions?: BrowserHttpOptions);
    destroy(): void;
    handle(request: HttpRequest<Blob>, options: HttpHandlerOptions): Promise<HttpResponse<Blob>>;
}
